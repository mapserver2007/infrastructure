- name: install sqlite3
  shell: >-
    /usr/bin/wget {{ sqlite_download_url }}{{ sqlite_arvhice_name }} &&
    /usr/bin/unzip {{ sqlite_arvhice_name }} &&
    mv -f sqlite3 /usr/bin/sqlite3
- name: copy sqlfile
  copy: "src={{ sqlite_sqlfile }} dest=/home/vagrant/setup"
- name: initialize table
  shell: "/usr/bin/sqlite3 /workspace/sqlite/{{ db_name }}.db < /home/vagrant/setup/{{ sqlite_sqlfile }}"
- name: change permission
  file: path=/workspace/sqlite/{{ db_name }}.db mode=0777 owner=vagrant group=vagrant state=file
