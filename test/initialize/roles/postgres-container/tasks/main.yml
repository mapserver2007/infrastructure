- name: copy docker file
  copy: "src=Dockerfile dest=/home/vagrant/{{ docker.postgres.label }}/"
- name: copy ansible file
  copy: "src=ansible dest=/home/vagrant/{{ docker.postgres.label }}/"
- name: docker build
  shell: docker build -t docker/postgres postgres-container
  sudo: yes
