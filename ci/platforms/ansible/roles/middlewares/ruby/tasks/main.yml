- name: docker build
  shell: docker build -t docker/ruby {{ ansible_env.PWD }}/roles/middlewares/ruby/files/Dockerfiles/ruby2.0/
  tags: ruby2.0
  sudo: yes
- name: docker build
  shell: docker build -t docker/ruby {{ ansible_env.PWD }}/roles/middlewares/ruby/files/Dockerfiles/ruby2.1/
  tags: ruby2.1
  sudo: yes
- name: docker build
  shell: docker build -t docker/ruby {{ ansible_env.PWD }}/roles/middlewares/ruby/files/Dockerfiles/ruby2.2/
  tags: ruby2.2
  sudo: yes
